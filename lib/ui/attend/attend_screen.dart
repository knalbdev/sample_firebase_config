import 'package:flutter/material.dart';

class AttendScreeen extends StatelessWidget {
  const AttendScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}